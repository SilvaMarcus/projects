let student = {
    name: 'ney',
    age: 25,
    weight: 16.5,
    isSubscribed: true,
}
/* console.log(student.name + ' de idade ' + student.age +' pesa '+ student.weight + 'quilos') 
console.log(`${student.name} de idade ${student.age} pesa ${student.weight} quilos`) */

let arrayVazia = [];

let carros = {
    corolla: 20,
    hyunday: 40,
    chevette:23,
}

let comidas = {
    carboidrato: 'feijao',
    proteina: 'frango',
    legumes: 'beringela',
}

arrayVazia = [
    student, carros, comidas
]

/* arrayVazia[2] =  comidas  // outro jeito de fazer 

 */

console.log(a)
var a = 1